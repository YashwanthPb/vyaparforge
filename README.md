VyaparForge: Manufacturing ERP 🏭VyaparForge is a specialized ERP and billing application designed for Shri Shakthi Industries. It manages inventory, purchase orders (POs), and billing specifically for HAL-tier manufacturing workflows.Built with a focus on privacy and local control, it is deployed via Docker on a private Hetzner VPS with fully automated CI/CD.🚀 Tech StackFramework: Next.js (Standalone mode)Database: PostgreSQL (Dockerized)ORM: PrismaDeployment: Docker ComposeAutomation: GitHub Actions (Auto-Deploy)🛠️ Local DevelopmentTo run this project on your Windows 11 machine (RTX 3050):Install Dependencies:Bashnpm install
Environment Setup: Create a .env file and add your DATABASE_URL.Run Prisma Migrations:Bashnpx prisma generate
npx prisma db push
Start Development Server:Bashnpm run dev
🌐 Production DeploymentThe application is hosted on a Hetzner VPS (Ubuntu 24.04). It uses a multi-stage Docker build to ensure a small, secure production image.Manual Sync (If needed)If the auto-deploy is ever paused, you can sync the server manually via SSH:Bashssh root@167.235.141.107
cd /root/vyaparforge
git pull origin main
docker compose up -d --build
🤖 CI/CD Workflow (Auto-Deploy)This project uses GitHub Actions for continuous deployment. Every push to the main branch triggers the following:Checkout: Pulls the latest code from GitHub.Handshake: Authenticates with the Hetzner server using SSH_PRIVATE_KEY.Deploy: Executes git pull and docker compose on the remote server.GitHub Secrets Required:SecretDescriptionSERVER_IPThe IP address of the Hetzner VPS.SERVER_USERUsually root.SSH_PRIVATE_KEYThe OpenSSH private key for server access.📂 Project Structure/app: Next.js application logic and UI./prisma: Database schema and seed scripts./.github/workflows: Auto-deployment scripts (YAML).Dockerfile: Production build instructions for the container.🔐 Security & PrivacyDatabase: Runs in a private Docker network, not exposed to the public internet.SSH: Restricted to key-based authentication only.Next.js: Running in standalone mode for optimal security and performance.
